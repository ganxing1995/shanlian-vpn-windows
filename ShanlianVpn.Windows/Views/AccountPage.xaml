<Page x:Class="ShanlianVpn.Windows.Views.AccountPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{StaticResource AppBackgroundBrush}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="0,0,8,0">
            <TextBlock Text="账户"
                       FontSize="28"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource BrightTextBrush}"/>
            <TextBlock Text="查看登录状态、设备和安全支持操作。"
                       Foreground="{StaticResource SoftTextBrush}"
                       Margin="0,6,0,22"/>

            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="登录账户" Foreground="{StaticResource SoftTextBrush}"/>
                    <TextBlock x:Name="EmailTextBlock"
                               FontSize="20"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource BrightTextBrush}"
                               Margin="0,8,0,22"/>

                    <TextBlock x:Name="DeviceSummaryTextBlock"
                               Text="设备"
                               Foreground="{StaticResource SoftTextBrush}"/>
                    <StackPanel x:Name="DevicesStackPanel" Margin="0,10,0,22"/>

                    <Grid Margin="0,0,0,22">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Background="#15243C" BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,8,0">
                            <StackPanel>
                                <TextBlock Text="客服支持" Foreground="{StaticResource SoftTextBrush}"/>
                                <TextBlock Text="请通过官方客服渠道联系支持"
                                           Foreground="{StaticResource BrightTextBrush}"
                                           Margin="0,8,0,0"/>
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="1" Background="#15243C" BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="16" Margin="8,0,0,0">
                            <StackPanel>
                                <TextBlock Text="版本" Foreground="{StaticResource SoftTextBrush}"/>
                                <TextBlock x:Name="VersionTextBlock"
                                           Foreground="{StaticResource BrightTextBrush}"
                                           Margin="0,8,0,0"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <Button Content="复制安全诊断"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Margin="0,0,0,10"
                            Click="CopyDiagnosticsButton_Click"/>
                    <Button Content="修改密码"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Background="#1E3354"
                            Margin="0,0,0,10"
                            Click="ChangePasswordButton_Click"/>
                    <Button Content="退出登录"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Background="{StaticResource DangerBrush}"
                            Click="LogoutButton_Click"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
