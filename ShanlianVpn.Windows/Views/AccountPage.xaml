<Page x:Class="ShanlianVpn.Windows.Views.AccountPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{DynamicResource AppBackgroundBrush}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="0,0,8,0">
            <TextBlock Text="账号"
                       FontSize="28"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource BrightTextBrush}"/>
            <TextBlock Text="查看登录状态、管理设备和处理账号操作。"
                       Foreground="{DynamicResource SoftTextBrush}"
                       Margin="0,6,0,22"/>

            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <Grid Margin="0,0,0,22">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel>
                            <TextBlock Text="登录状态" Foreground="{DynamicResource SoftTextBrush}"/>
                            <TextBlock x:Name="LoginStateTextBlock"
                                       Text="已登录"
                                       FontSize="20"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource BrightTextBrush}"
                                       Margin="0,8,0,0"/>
                            <TextBlock x:Name="EmailTextBlock"
                                       Foreground="{DynamicResource SoftTextBrush}"
                                       Margin="0,8,0,0"/>
                        </StackPanel>

                        <Border Grid.Column="1"
                                Background="#143A2A"
                                BorderBrush="#1F7A4A"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="12,6"
                                VerticalAlignment="Top">
                            <TextBlock Text="安全保护中"
                                       Foreground="#86EFAC"
                                       FontWeight="SemiBold"/>
                        </Border>
                    </Grid>

                    <UniformGrid Columns="1" Margin="0,0,0,22">
                        <Button Content="退出登录"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Background="{StaticResource DangerBrush}"
                                Margin="0"
                                Click="LogoutButton_Click"/>
                    </UniformGrid>

                    <TextBlock x:Name="DeviceSummaryTextBlock"
                               Text="设备"
                               Foreground="{DynamicResource SoftTextBrush}"/>
                    <StackPanel x:Name="DevicesStackPanel" Margin="0,10,0,22"/>

                    <Grid Margin="0,0,0,22">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,8,0">
                            <StackPanel>
                                <TextBlock Text="账号安全" Foreground="{DynamicResource SoftTextBrush}"/>
                                <Button Content="修改密码"
                                        Style="{StaticResource PrimaryButtonStyle}"
                                        Margin="0,12,0,0"
                                        Click="ChangePasswordButton_Click"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>

